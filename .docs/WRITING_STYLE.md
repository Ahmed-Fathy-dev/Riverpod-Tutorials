<div dir="rtl">

# دليل النمط الكتابي للمشروع

## 🎯 الأسلوب العام

### اللغة
- **النصوص التوضيحية**: عامية مصرية طبيعية ومبسطة
- **المصطلحات التقنية**: إنجليزي (مع ترجمة عربية في القوسين عند الحاجة)
- **التعليقات في الكود**: إنجليزي فقط

---

## 📝 قواعد الكتابة بالعامية المصرية

### ✅ استخدم

| بدلاً من (فصحى) | استخدم (عامية) |
|-----------------|----------------|
| ما هو | إيه هو |
| لماذا | ليه |
| كيف | ازاي / إزاي |
| يجب أن | لازم |
| سوف / سـ | هـ (مثل: هنتعلم) |
| عندما | لما |
| لأن | عشان |
| الآن | دلوقتي |
| بدون | من غير |
| لا تستخدم | متستخدمش |
| يمكن أن | ممكن |
| هذا / ذلك | ده / دي |
| حاجات / أشياء | حاجات |
| كثير / كثيرة | كتير |

### أمثلة

```markdown
❌ سنتعلم في هذا القسم كيفية استخدام...
✅ هنتعلم في القسم ده ازاي نستخدم...

❌ يجب أن نفهم أولاً ما هو State
✅ لازم نفهم الأول إيه هو State

❌ عندما يتغير State
✅ لما State يتغير
```

---

## 🔄 اتجاه النص (RTL/LTR)

### القاعدة الذهبية

**كل سطر لازم يبدأ بكلمة عربية** عشان الاتجاه يكون من اليمين لليسار.

### المشكلة

```markdown
❌ Mutations هي ميزة جديدة
   (هيبدأ من الشمال عشان بدأ بكلمة إنجليزي)

✅ ميزة Mutations هي ميزة جديدة
   (هيبدأ من اليمين عشان بدأ بكلمة عربي)
```

### الحلول

#### الحل 1: إضافة كلمة عربية في البداية

```markdown
❌ State Management هو نمط معماري
✅ إدارة الحالة (State Management) هو نمط معماري
```

#### الحل 2: إضافة "الـ" أو كلمة وصف

```markdown
❌ Provider يُستخدم لـ...
✅ الـ Provider بيُستخدم لـ...

❌ Widget هيتعمله rebuild
✅ واحد Widget هيتعمله rebuild
```

#### الحل 3: إعادة صياغة الجملة

```markdown
❌ Testing صعب لما State في UI
✅ الاختبار (Testing) صعب لما State يكون في UI
```

#### الحل 4: استخدام "حاجات زي" أو "ميزات زي"

```markdown
❌ Mutations, Offline Persistence, Automatic Retry
✅ ميزات زي Mutations و Offline Persistence و Automatic Retry

❌ Element System, Pointer Indirection, Scheduler
✅ تفاصيل زي Element System و Pointer Indirection و Scheduler
```

---

## 💻 أمثلة الكود

### القواعد

1. **التعليقات**: إنجليزي فقط
2. **الاتجاه**: LTR (من الشمال لليمين)
3. **الترقيم**: استخدم أرقام إنجليزية (1, 2, 3)

### مثال صحيح

```markdown
<div dir="rtl">

الكود ده بيعمل إيه؟

</div>

\`\`\`dart
// Create a provider for the counter
final counterProvider = StateProvider<int>((ref) {
  return 0; // Initial value
});

// Read the provider
final count = ref.watch(counterProvider);
\`\`\`

<div dir="rtl">

الـ Provider ده بيحتفظ بقيمة العداد.

</div>
```

---

## 📋 القوائم (Lists)

### قوائم غير مرقمة

```markdown
<div dir="rtl">

الأنواع المختلفة:
- نوع Provider العادي
- نوع FutureProvider للبيانات غير المتزامنة
- نوع StreamProvider للـ Streams

</div>
```

### قوائم مرقمة

```markdown
<div dir="rtl">

الخطوات:

</div>

\`\`\`
1. افتح Terminal
2. نفذ الأمر flutter pub get
3. شغل التطبيق
\`\`\`

<div dir="rtl">

كده خلصنا!

</div>
```

---

## 📊 الجداول

### استخدم Headers بالعربي

```markdown
<div dir="rtl">

| الميزة | StatefulWidget | Riverpod |
|--------|----------------|----------|
| **مكان الـ State** | جوا Widget | بره Widget |
| **إعادة الاستخدام** | صعبة | سهلة جداً |
| **Testing** | معقد | سهل جداً |

</div>
```

---

## 🔤 المصطلحات التقنية

### القاعدة العامة

**المصطلحات التقنية بالإنجليزي** مع إضافة ترجمة عربية في القوسين عند أول ظهور.

### أمثلة

```markdown
✅ الـ Provider ده بيحتفظ بـ State
✅ استخدم ConsumerWidget عشان تقرأ الـ Provider
✅ الـ Lifecycle (دورة الحياة) بتبدأ من هنا
✅ إدارة الحالة (State Management) ضرورية
```

### مصطلحات شائعة

| المصطلح | الاستخدام |
|---------|-----------|
| State | State (أو: الحالة) |
| Provider | Provider |
| Widget | Widget |
| ref.watch | `ref.watch` (كود) |
| ref.read | `ref.read` (كود) |
| UI | UI (أو: واجهة المستخدم) |
| Testing | Testing (أو: الاختبار) |
| State Management | State Management (أو: إدارة الحالة) |

---

## 🎨 التنسيق

### العناوين

```markdown
<div dir="rtl">

# العنوان الرئيسي

## القسم الفرعي

### الموضوع

</div>
```

### الفقرات

```markdown
<div dir="rtl">

هذه فقرة نصية كاملة بالعربي. لازم تكون واضحة ومفهومة.
نستخدم أسلوب بسيط وطبيعي.

الفقرة التانية هنا. كل فقرة منفصلة.

</div>
```

### الأكواد المضمنة (Inline Code)

```markdown
<div dir="rtl">

استخدم `ref.watch` عشان تقرأ الـ Provider، واستخدم `ref.read` عشان تعدل عليه.

</div>
```

---

## ⚠️ أخطاء شائعة

### ❌ خطأ: بدء السطر بإنجليزي

```markdown
❌ Provider بيحتفظ بـ State
```

### ✅ صحيح: بدء السطر بعربي

```markdown
✅ الـ Provider بيحتفظ بـ State
```

---

### ❌ خطأ: خلط الفصحى بالعامية

```markdown
❌ يجب أن نتعلم ازاي نستخدم Provider
```

### ✅ صحيح: عامية متسقة

```markdown
✅ لازم نتعلم ازاي نستخدم Provider
```

---

### ❌ خطأ: تعليقات بالعربي في الكود

```markdown
\`\`\`dart
// انشئ Provider
final provider = ...
\`\`\`
```

### ✅ صحيح: تعليقات بالإنجليزي

```markdown
\`\`\`dart
// Create a provider
final provider = ...
\`\`\`
```

---

## 📚 أمثلة كاملة

### مثال 1: موضوع بسيط

```markdown
<div dir="rtl">

# إيه هو Provider؟

الـ Provider ده زي صندوق بيحتفظ بـ State بتاعك. لما تحب تقرأ الـ State، تستخدم `ref.watch`.

## ازاي نستخدمه؟

</div>

\`\`\`dart
// Create a provider
final counterProvider = StateProvider<int>((ref) => 0);

// Read the provider
final count = ref.watch(counterProvider);
\`\`\`

<div dir="rtl">

الكود ده بسيط وسهل. جرب تشغله وشوف النتيجة!

</div>
```

### مثال 2: قائمة مع مصطلحات

```markdown
<div dir="rtl">

## المميزات

الـ Riverpod بيوفر:
- سهولة في الاستخدام
- اختبار (Testing) أسهل
- أداء (Performance) أحسن
- إدارة حالة (State Management) مركزية

كل الميزات دي بتخلي التطبيق أفضل!

</div>
```

---

## ✅ Checklist قبل الكتابة

- [ ] كل النصوص التوضيحية بالعامية المصرية
- [ ] كل سطر يبدأ بكلمة عربية (لضمان RTL)
- [ ] المصطلحات التقنية بالإنجليزي
- [ ] التعليقات في الكود بالإنجليزي
- [ ] استخدام `<div dir="rtl">` للنصوص العربية
- [ ] الأمثلة كاملة وقابلة للتشغيل
- [ ] الأسلوب طبيعي وسلس (مش رسمي)

---

**ملاحظة**: الهدف إن القارئ يحس إنك بتشرحله كصاحبه، مش كأستاذ في محاضرة رسمية!

</div>
